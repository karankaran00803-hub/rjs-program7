# RJS-Practical7
Create a Simple React Application Using `React.createElement()`

## Objective
Create a simple React application that displays the message **"Welcome to React"** using the `React.createElement()` method.

## Problem Statement
Develop a React application that creates an `<h1>` element with the text **"Welcome to React"** and renders it inside the HTML element with the id `root`.

## Instructions

- Do modify `index.html` for including the CDN links.
- Write your solution in `index.js`.
- Use the following React methods:
  - `React.createElement()`
  - `ReactDOM.createRoot()`
  - `root.render()`
- Display the text exactly as:
  ```
  Welcome to React
  ```

## Expected Output

```
Welcome to React
```

## Files

```
.
├── index.html       
├── index.js         
├── package.json
├── tests
│   ├── test.js
│   └── test.sh
└── .github
    └── workflows
        └── classroom50.yml
```

## Submission Guidelines

1. Complete the program.
2. Commit your changes.
3. Push the repository to GitHub.
4. GitHub Classroom/Classroom50 will automatically evaluate your submission.

## Evaluation Criteria

| Criteria | Marks |
|----------|------:|
| Uses `React.createElement()` | 3 |
| Creates an `h1` element | 2 |
| Displays **"Welcome to React"** | 2 |
| Uses `ReactDOM.createRoot()` | 2 |
| Uses `root.render()` | 1 |
| **Total** | **10** |
